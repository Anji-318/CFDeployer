# CFDeployer v1.1.0 项目结构

```
CFDeployer/
├── App.xaml                        # WPF 应用入口，全局资源
├── App.xaml.cs                     # 应用启动类、值转换器
├── MainWindow.xaml                 # 主窗口 UI
├── MainWindow.xaml.cs              # 主窗口逻辑（配置/账户/模板/部署/Pages）
├── MainViewModel.cs                # 视图模型（部署命令封装）
├── CFDeployer.csproj               # 项目文件（.NET 8 WPF）
├── global.json                     # SDK 版本约束
├── app.ico                         # 应用图标
├── app.manifest                    # DPI 感知清单
├── build.bat                       # 编译脚本
├── README.md                       # 项目说明
├── 项目结构.md                     # 本文档
├── 使用说明.html                   # 详细使用说明（HTML）
│
├── Models/                         # 数据模型
│   ├── AccountGroup.cs             # 账户组模型
│   ├── AppData.cs                  # 应用全局数据
│   ├── DeployJob.cs                # 部署任务 + 矩阵项
│   ├── DeployTarget.cs             # 部署目标枚举（Worker/Pages）
│   ├── LogEntry.cs                 # 日志条目
│   ├── PagesProject.cs             # Pages 项目模型
│   ├── Profile.cs                  # 独立配置（Worker/Pages）
│   └── WorkerTemplate.cs           # Worker 模板 + Pages 模板字段
│
├── Services/                       # 业务服务
│   ├── DeployService.cs            # Worker 部署服务（代理中转）
│   ├── PagesDeployService.cs       # Pages Direct Upload 直连部署
│   ├── StorageService.cs           # JSON 数据持久化
│   └── WorkerCodeProcessor.cs      # 代码编码/解码/混淆/格式化
│
├── Controls/                       # 自定义控件
│   ├── CodeEditor.xaml             # 代码编辑器 XAML
│   └── CodeEditor.xaml.cs          # 代码编辑器逻辑
│
├── Dialogs/                        # 弹窗
│   ├── ProxyConfigDialog.xaml      # 代理配置弹窗
│   └── ProxyConfigDialog.xaml.cs   # 代理配置逻辑
│
└── Resources/                      # 资源文件
    └── app.ico                     # 窗口图标
```

## 模块说明

### 1. 模型层（Models）

| 文件 | 职责 |
|------|------|
| `DeployTarget.cs` | 定义部署目标：`Worker`、`Pages` |
| `PagesProject.cs` | Pages 项目数据模型，含项目名、分支、部署类型、静态目录、代码、环境变量 |
| `Profile.cs` | 独立配置，可同时表示 Worker 或 Pages 配置 |
| `DeployJob.cs` | 单个部署任务参数，以及矩阵部署项 `DeployMatrixItem` |
| `WorkerTemplate.cs` | Worker/Pages 模板，支持变量替换 |
| `AccountGroup.cs` | 账户组，含多个 `Account` |
| `AppData.cs` | 应用持久化数据总容器 |
| `LogEntry.cs` | 日志条目模型 |

### 2. 服务层（Services）

| 文件 | 职责 |
|------|------|
| `DeployService.cs` | 通过代理 Worker 部署 Cloudflare Worker；支持 Worker/Pages 混合批次，Pages 自动限流 |
| `PagesDeployService.cs` | 直连 `api.cloudflare.com` 部署 Pages，含项目创建、Deployment 创建、文件上传、429 退避重试 |
| `StorageService.cs` | 读写 `%AppData%/CFDeployer/data.json` |
| `WorkerCodeProcessor.cs` | Worker 代码的 Base64/Unicode/Hex 编解码、混淆、格式化、Minify |

### 3. UI 层

| 文件 | 职责 |
|------|------|
| `MainWindow.xaml` / `.xaml.cs` | 主界面：配置管理、账户组、Worker 模板、部署矩阵、Pages 项目管理、Pages 矩阵部署 |
| `MainViewModel.cs` | 绑定属性与部署命令 |
| `Controls/CodeEditor.*` | 代码编辑控件 |
| `Dialogs/ProxyConfigDialog.*` | 代理 Worker URL 和密钥配置 |

## 数据流

```
用户操作 → MainWindow.xaml.cs
              ↓
        Models（Profile / PagesProject / AccountGroup / WorkerTemplate）
              ↓
        StorageService（持久化到 data.json）
              ↓
        DeployService / PagesDeployService（调用 Cloudflare API）
              ↓
        日志 / 状态更新
```

## 版本信息

- **当前版本**: v1.1.0
- **目标框架**: .NET 8.0-windows
- **UI 框架**: WPF
